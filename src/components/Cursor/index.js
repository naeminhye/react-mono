import CursorProvider, { useMousePosition, CursorContext } from './Provider';
import CursorDecorator, { useCursorHandlers } from './Decorator';

export default {
  CursorProvider,
  CursorContext,
  CursorDecorator,
  useMousePosition,
  useCursorHandlers,
};
